import React from "react";

interface childProps {
  level: string;
  clear: boolean;
  gameover: boolean;
}

const EndMessage = (props: childProps) => {
  return (
    <div>
      EndMessage
    </div>
  );
};

export default EndMessage;
